<Window x:Class="GTrack.Dialog.Module.Views.DialogView" mc:Ignorable="d"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="GTrack Dialog"
        Icon="/GTrack.Dialog.Module;component/Assets/gtrack-logo.png"
        WindowStartupLocation="CenterOwner" WindowStyle="None"
        SizeToContent="WidthAndHeight" ResizeMode="CanMinimize"
        AllowsTransparency="True" Background="Transparent">
    <Window.Template>
        <ControlTemplate TargetType="Window">
            <Border Background="{DynamicResource ApplicationBackgroundBrush}"
                    BorderBrush="{DynamicResource TextFillColorSecondaryBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <ui:TitleBar Grid.Row="0"
                                 Title="GTrack Dialog"
                                 Height="32"
                                 ShowMinimize="False"
                                 ShowMaximize="False"
                                 ShowClose="True">
                        <ui:TitleBar.Icon>
                            <ui:ImageIcon Source="pack://application:,,,/GTrack.Dialog.Module;component/Assets/gtrack-logo.png"/>
                        </ui:TitleBar.Icon>
                    </ui:TitleBar>
                    
                    <ContentPresenter Grid.Row="1" ContentSource="Content"/>
                </Grid>
            </Border>
        </ControlTemplate>
    </Window.Template>
</Window>