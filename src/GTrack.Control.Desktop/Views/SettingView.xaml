<UserControl x:Class="GTrack.Control.Desktop.Views.SettingView" mc:Ignorable="d"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:appearance="clr-namespace:Wpf.Ui.Appearance;assembly=Wpf.Ui"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             Background="{DynamicResource ApplicationBackgroundBrush}"
             Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="ApplicationThemes"
                            MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="appearance:ApplicationTheme" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <Grid Margin="15">
        <StackPanel Margin="10">
            <TextBlock Text="Настройки"
                       FontSize="24"
                       FontWeight="Bold"/>
            
            <ui:CardControl Margin="0,10,0,0" Icon="{ui:SymbolIcon Color24}">
                <ui:CardControl.Header>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <ui:TextBlock
                            Grid.Row="0"
                            FontTypography="Body"
                            Text="Тема приложения"/>
                        <ui:TextBlock
                            Grid.Row="1"
                            Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                            Text="Выберите тему приложения для отображения"/>
                    </Grid>
                </ui:CardControl.Header>
                <ComboBox MinWidth="200"
                          ItemsSource="{Binding Themes}"
                          SelectedItem="{Binding CurrentApplicationTheme, Mode=TwoWay}" />
            </ui:CardControl>
        </StackPanel>
    </Grid>
</UserControl>
